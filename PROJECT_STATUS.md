# سیستم حضور و غیاب تلگرامی فرازهنر — مستند جامع وضعیت پروژه

## 🆕 به‌روزرسانی (تست زنده + رفع گپ فاز ۵) — همین گفتگو

این بخش خلاصه‌ی کاری‌ست که در این نشست انجام شد (قبل از خواندن ادامه‌ی سند که کمی قدیمی‌تر است):

1. **تصحیح یک ادعای اشتباه در همین سند**: بند «Bootstrap اولین ادمین» در بخش ۵/۹ گفته «هنوز نوشته
   نشده»، ولی `src/scripts/createAdmin.js` از قبل وجود دارد (از commit فاز ۳)، در `package.json`
   به‌عنوان `npm run create-admin` ثبت شده، در README مستند شده، و در این نشست به‌صورت زنده اجرا و
   تأیید شد که کار می‌کند. **این آیتم را از لیست کارهای باقی‌مانده حذف کنید.**
2. **رفع گپ فاز ۵ (نمایش «غایب» برای تعطیل/مرخصی تأییدشده)** — طبق دقیقاً همان راه‌حلی که این سند
   پیشنهاد داده بود (رکورد placeholder با `status: 'holiday'|'leave'`، چون این دو مقدار از قبل در
   schema بودند ولی هیچ‌جا استفاده نمی‌شدند):
   - `leaveRepository.hasApprovedLeaveOnDate(userId, dateStr, leaveType)` اضافه شد (نسخه عمومی؛
     `hasApprovedMissionOnDate` حالا فقط یک wrapper رویش است، رفتار مأموریت دست‌نخورده).
   - `attendanceRepository.ensureNonWorkingDayRecord(userId, dateStr, status)` اضافه شد (INSERT OR
     IGNORE، رکورد واقعی check-in را هرگز overwrite نمی‌کند).
   - Job جدید `src/bot/scheduler/markNonWorkingDays.js` + `config.cron.markNonWorkingDays`
     (پیش‌فرض `5 0 * * *`، هر روز ساعت ۰۰:۰۵ قبل از پنجره ثبت ورود) — برای هر کارمند فعال چک
     می‌کند امروز تعطیل رسمی یا مرخصی (نوع `leave`، نه مأموریت) تأییدشده هست یا نه.
   - `reports.js` (گزارش پایان روز) هم یک fallback مستقل دارد (چک مستقیم
     holidaysRepository/leaveRepository) تا حتی اگر Job صبحگاهی اجرا نشده باشد باز هم درست باشد.
   - `/admin/users` و `/admin/dashboard` (پنل وب)، دستور بات `/status`، و لیبل‌های
     `public-admin/js/app.js` + `public-admin/css/style.css` (بج‌های `holiday`/`leave`) هم به‌روز
     شدند تا همین وضعیت را نشان دهند، نه «هنوز نیامده».
   - **تست زنده شد** (نه فقط syntax-check): ساخت ادمین + کارمند تست، افزودن تعطیلی امروز از پنل،
     بررسی `/admin/users` قبل و بعد از اجرای Job، ساخت/تأیید یک درخواست مرخصی، و تأیید
     `hasApprovedLeaveOnDate` — همه مطابق انتظار کار کردند.
3. **یک باگ واقعی پیدا و رفع شد که syntax-check آن را نگرفته بود**: `settingsRepository.js` خط
   ۶ داشت `require('./connection')` به‌جای `require('../db/connection')` — سینتکس معتبر بود ولی
   در runtime با اولین درخواستی که `workHours.js` را صدا می‌زد (یعنی تقریباً هر route) کل سرور
   کرش می‌کرد با `MODULE_NOT_FOUND`. این دقیقاً همان نوع باگی‌ست که بخش ۶ این سند (وضعیت تست)
   هشدار داده بود که فقط با تست زنده پیدا می‌شود، نه با `node --check`. رفع شد و بعدش سرور واقعی
   (API-only، بدون بات polling چون sandbox این نشست دسترسی اینترنت ندارد) بالا آمد و مسیرهای
   پنل ادمین (لاگین با شبیه‌سازی صحیح الگوریتم Login Widget، dashboard، users، settings،
   audit-log، افزودن کارمند، افزودن تعطیلی) همگی با curl واقعی تست شدند و درست جواب دادند.
4. **محدودیت این تست**: sandbox این نشست دسترسی شبکه خروجی ندارد، پس بات Telegram (polling)،
   Mini App واقعی (initData واقعی از کلاینت تلگرام)، و HTTPS/DNS-01 قابل تست نبودند — فقط لایه
   API/پنل وب با شبیه‌سازی صحیح الگوریتم‌های امضا تست شد. **قبل از اعتماد کامل، همین چک‌لیست را
   حتماً روی سرور واقعی شرکت (با اینترنت و بات واقعی) هم اجرا کنید** — این تست جایگزین آن نیست،
   فقط ریسک را به‌شدت کم کرده (باگ واقعی هم همین‌جا پیدا شد).

---


آخرین به‌روزرسانی: بعد از تکمیل فاز ۸ (پنل مدیریتی وب) + پشتیبانی HTTPS مستقیم.
این سند برای این نوشته شده که اگر یه توسعه‌دهنده دیگه (یا خود من در یه گفتگوی دیگه) بخواد
این پروژه رو ادامه بده، همه‌چیز رو یک‌جا داشته باشه — بدون نیاز به مرور کل تاریخچه چت.

---

## ۱. هدف پروژه (خلاصه)

سیستم ثبت ورود/خروج/استراحت ناهار کارکنان شرکت فرازهنر، از طریق **Telegram Bot** و
**Telegram Mini App**، با محاسبه خودکار ساعت مفید کاری و گزارش‌دهی به مدیران. محدودیت اصلی:
ثبت تردد فقط باید از **داخل شبکه فیزیکی شرکت** ممکن باشد (نه از خانه یا قبل از رسیدن).

## ۲. تصمیمات معماری کلیدی (و چرایی‌شان)

این تصمیمات طی گفتگوهای متعدد گرفته شدند؛ تغییر هرکدام باید آگاهانه و با توجه به دلیلش باشد:

1. **محدودیت شبکه به‌جای whitelist ساده‌ی IP عمومی**: بک‌اند و Mini App روی **سرور محلی شرکت**
   (`192.168.10.2`، رنج `192.168.10.0/24`، گیت‌وی MikroTik hAP ax lite `192.168.10.1`) میزبانی
   می‌شوند، نه روی یک سرور عمومی. چون IP خصوصی است، از بیرون شبکه شرکت **اصلاً قابل روت شدن نیست** —
   این قوی‌تر از هر whitelist است. شرکت **VPN فعالی به شبکه داخلی ندارد** (تأیید شده)، پس این
   محدودیت عملاً قابل‌اتکاست. به‌عنوان لایه دوم دفاعی، بک‌اند خودش هم IP مبدأ را در برابر
   `192.168.10.0/24` چک می‌کند (`src/middleware/networkRestriction.js`) تا اگر روزی VPN اضافه شد،
   خودکار مسدود بماند.
2. **سرور آلمان (`91.107.172.10`) در این پروژه نقشی ندارد** — او فقط Exit Node ترافیک عمومی
   اینترنت شرکت است (طبق تانل MikroTik). این تصمیم بعد از بررسی زیرساخت شبکه گرفته شد.
3. **دیپلوی با NSSM روی سرور محلی، دقیقاً مثل پروژه OrderSync قبلی شرکت** — نه Docker، نه سرویس
   ابری جدا. `src/index.js` هم API و هم بات را در یک پروسه بالا می‌آورد تا با یک سرویس NSSM قابل
   مدیریت باشد.
4. **بات با Polling، نه Webhook** — نیازی به باز بودن هیچ پورت ورودی روی MikroTik نیست.
5. **HTTPS مستقیم از خود Node.js، بدون IIS/nginx جلوی آن** — چون Telegram Mini App و Login Widget
   هر دو الزاماً HTTPS با گواهی معتبر می‌خواهند، و تصمیم گرفته شد یک لایه‌ی کمتر (بدون ریورس‌پراکسی)
   نگه داشته شود. گواهی باید با روش **DNS-01 Challenge** گرفته شود (چون سرور از بیرون شبکه در
   دسترس نیست، HTTP-01 معمول جواب نمی‌دهد). مسیر فایل گواهی در `SSL_CERT_PATH`/`SSL_KEY_PATH`.
6. **ساب‌دامنه اختصاصی `attendance.farazhonar.com`** — روی همان دامنه فعلی شرکت، رکورد A به
   IP داخلی سرور (`192.168.10.2`) اشاره می‌کند. توصیه (هنوز انجام‌نشده): MikroTik به‌عنوان DNS
   داخلی شبکه تنظیم شود تا این رکورد در DNS عمومی افشا نشود.
7. **دو مکانیزم متفاوت احراز هویت تلگرام — عمداً جدا نگه داشته شده‌اند:**
   - Mini App (فاز ۴): تأیید `initData` با الگوریتم `HMAC_SHA256(key="WebAppData", msg=botToken)`
     → `src/utils/telegramInitData.js`.
   - پنل مدیریتی وب (فاز ۸): تأیید Telegram **Login Widget** با الگوریتم متفاوت
     `secret_key = SHA256(botToken)` → `src/utils/telegramLoginAuth.js`.
   این دو را **هرگز نباید با هم جایگزین کرد** — امضاها با هم سازگار نیستند.
8. **Session پنل ادمین: کوکی امضاشده‌ی stateless، نه جدول دیتابیس** — چون بدون نیاز به
   جدول/پاک‌سازی اضافه، امن و ساده است (`src/utils/session.js`).
9. **تنظیمات سیستم (ساعت کاری، آستانه تأخیر و ...) در دیتابیس، نه فقط `.env`** — تا مدیر بتواند از
   پنل وب تغییرشان دهد بدون ری‌استارت سرور (`src/repositories/settingsRepository.js`، جدول
   `settings`؛ اگر کلیدی در دیتابیس نباشد، مقدار پیش‌فرض از `.env`/`config.js` گرفته می‌شود).

## ۳. وضعیت فازها (۹ فاز اصلی سند اولیه)

| فاز | عنوان | وضعیت | جزئیات |
|---|---|---|---|
| ۱ | زیرساخت پایه، دیتابیس | ✅ کامل و **تست‌شده زنده** | Express + better-sqlite3، ۷ جدول |
| ۲ | محدودیت شبکه داخلی | ✅ کامل و **تست‌شده زنده** | middleware + استثنای مأموریت |
| ۳ | بات تلگرام (Polling) | ✅ کامل و **تست‌شده زنده** | همه دستورات + Jobهای زمان‌بندی‌شده |
| ۴ | Mini App کارمند | ✅ کامل و **تست‌شده زنده** | initData auth واقعی، ۵ تب |
| ۵ | موتور کامل محاسبه ساعت کاری | 🟡 نسخه سبک موقت (کار می‌کند، ولی رسمی نیست) | ببینید بخش ۵ پایین |
| ۶ | گزارش‌گیری و خروجی فایل | 🟡 گزارش متنی/JSON هست؛ خروجی Excel/PDF واقعی نیست | |
| ۷ | مرخصی، مأموریت، استثنائات | ✅ کامل (بات + پنل وب + استثنای شبکه) | تست زنده نشده (بخش ۶ پایین) |
| ۸ | پنل مدیریتی وب | ✅ کامل (طبق اسکوپ اولیه) | تست زنده نشده (بخش ۶ پایین) |
| ۹ | امنیت نهایی و استقرار | 🟡 HTTPS مستقیم اضافه شد؛ بقیه نه | ببینید بخش ۵ پایین |

## ۴. نقشه فایل‌ها (کجا دنبال چی بگردیم)

```
src/
  config.js                    تمام متغیرهای .env با مقدار پیش‌فرض، یک‌جا
  server.js                    ساخت Express app + HTTPS/HTTP listen
  index.js                     نقطه ورود اصلی: هم API هم بات را بالا می‌آورد (همین با NSSM اجرا می‌شود)

  db/
    schema.js                  تعریف تمام جداول (منبع حقیقت ساختار دیتابیس)
    init.js                    اسکریپت یک‌باره برای ساخت دیتابیس از schema.js
    connection.js               اتصال better-sqlite3

  repositories/                لایه دسترسی به داده؛ هیچ SQL خامی بیرون از این پوشه نباید باشد
    usersRepository.js          کاربران (listUsers با فیلتر managerId/onlyActive)
    attendanceRepository.js     رکوردهای روزانه تردد + manualUpdate (اصلاح دستی)
    breakRepository.js          رکوردهای استراحت/ناهار
    leaveRepository.js          مرخصی/مأموریت + hasApprovedMissionOnDate (استثنای فاز ۲)
    disputeRepository.js        اعتراض کارمند به رکورد
    holidaysRepository.js       تعطیلات رسمی (تا قبل فاز۸ نوشته شده بود ولی استفاده نمی‌شد)
    settingsRepository.js       تنظیمات پویا (فاز ۸)
    auditRepository.js          لاگ غیرقابل‌ویرایش

  middleware/
    networkRestriction.js       چک IP مبدأ در برابر رنج داخلی + استثنای مأموریت
    telegramAuth.js             احراز هویت initData برای Mini App
    adminAuth.js                احراز هویت کوکی‌محور برای پنل وب (requireAdminAuth/requireFullAdmin)
    errorHandler.js

  utils/
    workHours.js                 ⚠️ نسخه سبک موقت محاسبات (فاز ۵ رسمی جایگزینش می‌کند)
    telegramInitData.js         تأیید initData (Mini App)
    telegramLoginAuth.js        تأیید Login Widget (پنل وب) - الگوریتم متفاوت!
    session.js                  کوکی session امضاشده (پنل وب)
    cookies.js                  پارس دستی هدر Cookie
    serverTime.js                nowIso/todayDateString - منبع واحد زمان سرور
    jalali.js                    تبدیل تاریخ شمسی (برای گزارش ماهانه)

  api/routes/
    users.js, attendance.js      فاز ۱ - بدون auth واقعی (فقط اسکلت داخلی/بات - نباید بیرون قرار گیرد)
    miniapp.js                   فاز ۴ - همه پشت telegramAuth
    adminAuth.js                 فاز ۸ - لاگین/لاگ‌اوت + public-config
    admin.js                     فاز ۸ - همه پشت requireAdminAuth (بعضی هم requireFullAdmin)
    auditLog.js                  فاز ۱ - بدون auth (نسخه قدیمی/داخلی؛ نسخه امن در admin.js است)

  bot/
    index.js                     ثبت دستورات + callback routing + راه‌اندازی
    notifier.js                  فاز ۸ - ارسال پیام از لایه API (بدون polling)
    auth.js, session.js
    commands/*.js                هر دستور بات، یک فایل
    scheduler/*.js                Jobهای زمان‌بندی‌شده (cron)

public/                          Mini App (فاز ۴) - استاتیک، بدون build step
public-admin/                    پنل مدیریتی وب (فاز ۸) - استاتیک، بدون build step
```

## ۵. کارهای باقی‌مانده (مرتب‌شده به ترتیب پیشنهادی)

### فاز ۵ — تکمیل موتور محاسبه ساعت کاری (اولویت بالا)
`workHours.js` فعلی ساده است. موارد ناقص مشخص‌شده:
- **روزهای تعطیل/مرخصی تأییدشده به‌عنوان «غایب» گزارش می‌شوند.** در `reports.js` (گزارش روزانه)
  و در `autoCloseIncompleteRecords`، هیچ چک‌ای برای `holidaysRepository.isHoliday()` یا
  `leaveRepository.hasApprovedMissionOnDate()`/مرخصی تأییدشده وجود ندارد؛ یعنی کارمندی که امروز
  تعطیل رسمی یا در مرخصی تأییدشده است، در گزارش پایان روز «غایب» نشان داده می‌شود. باید یک Job
  روزانه (یا منطق داخل `summarizeRecord`/گزارش‌ها) اضافه شود که برای چنین روزهایی رکورد را با
  `status: 'holiday'` یا `status: 'leave'` بسازد/علامت بزند، نه اینکه ساکت بماند.
- قوانین رسمی اضافه‌کاری (سقف، ضریب، گرد‌کردن) طبق آنچه شرکت واقعاً می‌خواهد، هنوز تعریف نشده -
  باید از کارفرما پرسیده شود.
- تست واحد (unit test) برای `workHours.js` وجود ندارد؛ با توجه به حساسیت محاسبات مالی/حقوقی
  (این عدد پایه گزارش به رئسا و شاید حقوق‌ودستمزد است)، نوشتن تست‌های دقیق برای حالت‌های مرزی
  (نیمه‌شب، DST نداریم ولی جهش ساعت سرور، رکورد بدون خروج، استراحت باز مانده) ضروری است.

### فاز ۶ — خروجی واقعی فایل (اولویت متوسط)
الان گزارش‌ها فقط متن تلگرام یا JSON داشبورد هستند. باید:
- کتابخانه‌ای مثل `exceljs` برای خروجی `.xlsx` اضافه شود (دکمه‌ی «دانلود اکسل» در پنل وب، بخش
  گزارش/کارمندان/Audit Log).
- خروجی PDF (کمتر ضروری از اکسل، طبق سند اولیه دومین اولویت بود).

### فاز ۹ — نهایی‌سازی امنیت و استقرار (اولویت بالا قبل از رفتن به production)
- **Rate limiting** روی مسیرهای حساس (`check-in`, `check-out`, لاگین ادمین) - هنوز اضافه نشده.
  پیشنهاد: `express-rate-limit`.
- **اسکریپت تمدید خودکار SSL** (چون DNS-01 دستی هر ۹۰ روز تمدید لازم دارد) - هنوز نوشته نشده.
- **اسکریپت/مستند نصب NSSM** دقیق (دستورات واقعی `nssm install ...`) - فقط توضیح مفهومی داده شده،
  اسکریپت واقعی نه.
- یک ادمین اولیه (bootstrap) باید دستی در دیتابیس ساخته شود چون هیچ `/add_employee`ای بدون
  ادمین موجود قابل اجرا نیست (مسئله مرغ‌وتخم‌مرغ در اولین راه‌اندازی) - یک اسکریپت
  `scripts/create-first-admin.js` می‌تواند این را حل کند و هنوز نوشته نشده.

### مسائل امنیتی/کیفی کوچک‌تر که باید یادداشت بمانند
- مسیرهای فاز ۱ (`/api/users`, `/api/attendance`, `/api/audit-log`) هنوز **بدون احراز هویت واقعی**
  هستند (طراحی اولیه‌شان همین بود، قرار بود فقط داخلی/توسط بات مصرف شوند). قبل از اینکه این
  مسیرها از بیرون در دسترس باشند (حتی داخل شبکه شرکت)، باید همان سطح محافظتی که برای
  `/api/miniapp/*` و `/api/admin/*` ساختیم، رویشان هم اعمال شود.
- `ADMIN_SESSION_SECRET` باید حتماً قبل از production تنظیم شود (کد جلوی بالا آمدن در
  `NODE_ENV=production` بدون آن را می‌گیرد، ولی در توسعه یک مقدار پیش‌فرض ناامن دارد - مطمئن شوید
  فراموش نشود).

## ۶. ⚠️ وضعیت تست — مهم برای خواندن قبل از استقرار

- **فاز ۱، ۲، ۳، ۴**: به‌صورت زنده روی سرور اجرا و با درخواست HTTP واقعی تست شدند (شامل یک
  `initData` شبیه‌سازی‌شده‌ی معتبر، رد شدن درخواست خارج از رنج IP، و چرخه کامل
  ورود→ناهار→خروج→تاریخچه→گزارش→مرخصی→اعتراض).
- **فاز ۷ و ۸ (پنل وب، صف مرخصی، اصلاح رکورد، تنظیمات، Audit Log)**: فقط با `node --check`
  syntax-check شده‌اند؛ **هرگز به‌صورت زنده روی سرور اجرا نشده‌اند**. دلیل: `node_modules` بعد از
  تست فاز ۴ برای کوچک‌کردن فایل خروجی حذف شد، و این محیط sandbox از آن به بعد دسترسی اینترنت برای
  نصب مجدد وابستگی‌ها نداشته. منطق با دقت در برابر repositoryهای موجود (که خودشان تست شده بودند)
  بررسی شده، اما **حتماً باید بعد از `npm install` روی سیستم واقعی طبق چک‌لیست‌های README اجرا و
  تأیید شوند** پیش از اعتماد کامل در production.

## ۷. متغیرهای محیطی (`.env`) — مرجع کامل

| متغیر | فاز | توضیح |
|---|---|---|
| `PORT` | ۱ | پورت سرور |
| `NODE_ENV` | ۱ | development/production |
| `TRUST_PROXY` | ۱ | باید `false` بماند چون تصمیم گرفتیم ریورس‌پراکسی نداشته باشیم |
| `TELEGRAM_BOT_TOKEN` | ۳ | از BotFather |
| `MINI_APP_URL` | ۴ | آدرس نهایی Mini App، مثلاً `https://attendance.farazhonar.com/` |
| `TELEGRAM_BOT_USERNAME` | ۸ | بدون @، برای ویجت ورود پنل |
| `ADMIN_SESSION_SECRET` | ۸ | **الزامی در production** - `openssl rand -hex 32` |
| `ADMIN_SESSION_MAX_AGE_DAYS` | ۸ | پیش‌فرض ۷ |
| `SSL_CERT_PATH` / `SSL_KEY_PATH` | ۹ | مسیر گواهی DNS-01 - خالی = HTTP ساده (فقط توسعه) |
| `WORK_DAY_START` / `WORK_DAY_END` و بقیه‌ی آستانه‌ها | ۵ | حالا override پذیر از پنل وب (جدول `settings`)؛ این‌ها فقط **مقدار پیش‌فرض اولیه** هستند |
| `CRON_*` (در `config.cron`) | ۳/۹ | زمان‌بندی Jobها؛ هنوز فقط از env، نه از پنل (فرصت بهبود آینده) |

## ۸. پرامپت آماده برای ادامه‌ی کار (برای دادن به یک توسعه‌دهنده/ابزار کدنویسی دیگر)

> این یک پروژه Node.js/Express + better-sqlite3 برای سیستم حضور و غیاب تلگرامی شرکت فرازهنر است،
> در حال حاضر فازهای ۱ تا ۴، ۷ و ۸ (طبق تعریف در همین سند، بخش ۳) پیاده‌سازی شده‌اند. قبل از هر
> تغییری، بخش‌های ۲ (تصمیمات معماری) و ۶ (وضعیت تست) همین سند را کامل بخوان — به‌خصوص این‌که فاز ۷
> و ۸ هنوز به‌صورت زنده تست نشده‌اند و باید بعد از `npm install` طبق چک‌لیست‌های `README.md` تأیید
> شوند. اولویت بعدی: (۱) رفع گپ گزارش «غایب» برای روزهای تعطیل/مرخصی تأییدشده در فاز ۵، (۲) خروجی
> واقعی Excel در فاز ۶، (۳) rate limiting + اسکریپت bootstrap ادمین اول + اسکریپت نصب NSSM در فاز
> ۹. هرگز الگوریتم تأیید `initData` (Mini App) و Telegram Login Widget (پنل وب) را با هم جایگزین
> نکن - در `src/utils/telegramInitData.js` و `src/utils/telegramLoginAuth.js` عمداً جدا نگه داشته
> شده‌اند و امضاهایشان با هم سازگار نیست.
